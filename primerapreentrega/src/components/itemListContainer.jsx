import { useParams } from "react-router-dom";

const ItemListContainer = () => {
    const { id } = useParams();
    return (  
        <>
            
        </>
    );
}
 
export default ItemListContainer;